# Build context теперь только app/, поэтому .git/.claude/*.md/*.zip/data
# репозитория сюда физически не попадают вообще — фильтровать их не нужно.
# Осталось только то, что реально может появиться внутри app/ при разработке.
__pycache__
*.pyc
.venv
